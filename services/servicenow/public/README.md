# Icon goes here

Drop the brand icon in this folder as `icon.svg` (preferred) or `icon.png`, matching the
`@integrationIcon` reference in `../src/index.js`.

The real brand asset is a human step — the build agent references the path but does not
generate or commit the binary (see docs/ai/extension-build-playbook.md, §9).
